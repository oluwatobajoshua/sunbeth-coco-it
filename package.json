{
  "name": "coco-issue-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.x"
  }
}
